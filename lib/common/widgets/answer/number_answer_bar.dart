import 'package:flutter/material.dart';

class NumberAnswerBar extends StatelessWidget {
  const NumberAnswerBar({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
